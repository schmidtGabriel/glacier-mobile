import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1976D2);
  static const primaryLight = Color(0xFF63A4FF);
  static const secondary = Color.fromARGB(255, 234, 68, 88);
  static const secondaryLight = Color.fromARGB(255, 255, 102, 102);

  // Light Theme Colors
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFF3F3F3);
  static const lightOnBackground = Color(0xFF1C1C1E);
  static const lightOnSurface = Color(0xFF1C1C1E);
  static const lightOnSurfaceVariant = Color(0xFF666666);

  // Dark Theme Colors
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkSurfaceVariant = Color(0xFF2C2C2C);
  static const darkOnBackground = Color(0xFFE1E1E1);
  static const darkOnSurface = Color(0xFFE1E1E1);
  static const darkOnSurfaceVariant = Color(0xFFB3B3B3);

  // Accent colors for both themes
  static const lightDivider = Color(0xFFE0E0E0);
  static const darkDivider = Color(0xFF383838);

  static const lightShadow = Color(0x1A000000);
  static const darkShadow = Color(0x40000000);
}
